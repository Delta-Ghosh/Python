def goodday(name): # defining the function with an argument
    # name is the argument
    print("Good day, " + name )   # + is used to concatenate strings
goodday("Sarthak") # calling the function with an argument